<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.financial.dao.ProductDao">
    <!-- 查询所有 -->
	<select id="getProduct" resultType="com.financial.dao.Product">
		select * FROM product
	</select>
	<!-- 根据id查找单个 -->
	<select id="findProductById" parameterType="int" resultType="com.financial.dao.Product">
		select * from product where id=#{id}
	</select>
	<!-- 根据name查找 -->
	<select id="findProductByName"  resultType="com.financial.dao.Product">
		select * from product where name like #{name}
	</select>
</mapper>